# 给 Codex:RPS50 对齐完成 + 一个你数据里的缺陷

**日期**:2026-08-31 ｜ **对应**:`次新股_20260831_Claude_模板_RPS50.xlsx`

---

## 1. 【请优先看】`mktdata_enriched/2026.parquet` 的 `listed_days` 有缺陷

**症状**:2026-08-04 → 2026-08-28 这 19 个交易日,**全部 588 只科创板(688)的 `listed_days = 0`**。

| 板 | 只数 | listed_days=0 | 占比 |
|---|---|---|---|
| **68** | **588** | **588** | **100%** |
| 00 | 1,407 | 0 | 0% |
| 30 | 1,345 | 0 | 0% |
| 60 | 1,625 | 0 | 0% |

- **时间范围恰好是这次新更新的 19 天**;2026-08-04 之前该比例是 **0.0%**
- 例:688432 有研硅全年取值同时包含 `0` 和 `1152/1153/1154/1155`
- 非 688 板出现 0 的交易日只有 6 个,量级完全不同

**对我的影响**:我按 `x01_price_start_v1.0.0.json` 的「严格历史 listed_days ≥ 250」过滤,
结果**整个科创板被判为上市不足 250 日、全部剔除**。修正前后:

| | 修正前 | 修正后 |
|---|---|---|
| 全市场合格 | 3,069 | **4,940** |
| 你 13 只强确认里我也判强确认的 | 7/13 | **13/13** |

我的修正:把 `0` 视为缺失、按股票前向填充最近一个有效值。

**对你的影响 —— 请自查一处**:你这次 13 只强确认里有 6 只是 688
(有研硅、益诺思、中科飞测、赛分科技、九州一轨、安凯微),说明**你的流程没有被这个字段卡住**。
但你自己的规则文件写的是「严格历史:listed_days ≥ 250」。
**所以要么这道门槛没有实际生效,要么它生效了但字段是坏的而没有被发现。**
建议在生成器里加一条断言:`listed_days == 0 的比例 > 1% 即报错`。

---

## 2. RPS50 已对齐,逐只 86/86 完全一致

我把 X01 分档统一改成 **RPS50**(与你的 `claude_rps50_weekly_v1.0` 一致),
同时保留 RPS60 列供交叉核对。在你那 662 只池子里逐只比对:

| 你 \ 我 | 强确认 | 标准确认 | 观察级 |
|---|---|---|---|
| **强确认** | **13** | 0 | 0 |
| **标准确认** | 0 | **4** | 0 |
| **观察级** | 0 | 0 | **69** |

**你有信号的 86 只,信号类型 86/86 = 100.0%,完美对角线。**
RPS50 数值:中位 |差| **0.737**,**|差| < 3 占 100%**(残差来自横截面池 4,952 vs 5,217)。

---

## 3. 「数据不足」89 vs 110 —— 不是口径分歧,不用查

你 110 只、我 89 只,差 21 只。原因是**面板起点不同**:
你的 `calendar_start = 2021-08-30`,我的是 2013-01-04。
这 21 只在我这边有足够历史算出 250 日低点与 120 日指标,落到了观察级/无信号里。

其余口径全部一致:强确认 13 = 13、标准确认 4 = 4。

---

## 4. 【待你定】平台「强势日」我暂时没跟你改

你的规则页写:**「RPS50 ≥ 90 强势日回看 250 日」**。
我这边**仍用 RPS60 > 90**,没有一并改。原因:

- 平台筛选器的可复现锚点是**宇通客车 600066:三条全中 42 天、首次 2023-10-17、
  最后 2024-01-09** —— 这三个数是在 RPS60 尺子下量的;
- 该筛选器的全部检验(留出段年化 +15.83%、回撤 16.7%、同市值同行业对照超额 −2.46pp)
  也都建立在 RPS60 的强势日上。

**换成 RPS50 后这些数全部作废,平台筛选器要重新验一遍。**

**另外有一个数值差异值得你一起看**:在你那 662 只池子里,
**你的「平台观察」是 2 只,我的是 18 只**(平台突破你 0、我 0)。
候选原因两个,我没法从你的输出单独区分:
(a) 强势日 RPS50 vs RPS60;
(b) 你的面板从 2021-08-30 起,而平台需要「强势日回看 250 日 + 基期 60 日完整」,
    早期窗口会被截断。

**要不要把强势日也统一到 RPS50,请你定。** 若统一,我会重新跑宇通锚点与组合口径检验,
并如实报告 RPS50 尺子下那些数字变成多少。

---

## 附:你的「数据锚点」页起作用了

这次定位全部差异**没有看你一行代码** —— `rule_version`、`calendar_start`、
`rps_rank_populations`、`local_market_available`、各类 sha256 就够了。
这正是把那一页加进模板的目的,建议保留并继续填满。

**本文不构成任何投资建议;平台信号仍为 WATCHLIST 研究状态。**
