"""第一六八节 事前登记:把平台「强势日」从 RPS60>90 统一到 RPS50>=90,重跑第一五五节(结果未跑)。

起因
----
用户指令「统一到 RPS50」。第一六六节已把 X01 分档改成 RPS50(与 Codex 的
`claude_rps50_weekly_v1.0` 一致),但平台强势日当时保留了 RPS60,理由是
宇通锚点与第一五五节的全部检验都建立在 RPS60 上。本节把这最后一处也统一,
**并如实报告第一五五节的数字在 RPS50 尺子下变成什么。**

第一六七节补测(`strongday_rps50_vs_rps60.py`)已量清楚代价:
全历史三条全中 1,414,428 → 1,409,206(−0.37%),突破买点 31,902 → 28,346;
2026-08-28 全市场平台观察 137 → 154;662 池内 19 → 15,**但只有 9 只重合**
(并集 25,个股一致率 9/25 = 36%)。**所以个股名单会大改,组合结论可能变。**

本节改了什么、没改什么
----------------------
**只改一处**:`platform_pivot.py` 里强势日矩阵的算法,由
`RPS60 > 90` 改为 `RPS50 >= 90`(`>=` 是 Codex 源码里的写法,不做人为对齐)。
做成 `OXQ_STRONG_N` 环境变量开关而**不是直接改掉**,因为第一五五节的全部数字
都挂在 RPS60 上,直接改会让那一节静默失去可复现性。不设变量时逐字复现第一五五节。

**其余一个字不改**:面板 (3297, 5232)、legacy 三阈值、调整天数≥15、
强势日回看≤250、触线日要求 MA100 向上、突破买入、止损挂平台下沿且上限 15%、
10 个等权槽位、收敛比升序填槽、大盘 MA200 过滤、
训练段 2019-01–2022-12 只报数、留出段 2023-01–2026-04 判据、
500 组同市值名次±25 同申万一级行业对照、零成本判定。

判据(跑之前写死,跑完照判,不放宽;加严可以)
--------------------------------------------
C1 锚点(不过则本节作废)
   (a) 面板 (3297, 5232);(b) ffill 后首价之后无空洞;(c) 行业恒等式违例 = 0;
   (d) 无前视:3,000 个真实买点逐点断言上沿/下沿/平台起点,大盘 MA200 抽查;
   (e-3) **换尺子的实现恒等**:同一条代码路径在 `STRONG_N=60` 下重算的强势日矩阵
         必须与 `load_panel` 返回的**逐点相等**。**不过则本节作废** ——
         这是唯一能证明「只有尺子变了、实现没动」的东西。
   **(e-2) 宇通 42 天锚点在本节降为描述项**:那三个数是 RPS60 尺子量的,
   换尺子后必然不成立,**不成立不算作废**,但**必须把 RPS50 下的三个数报出来**。

C2 **主判据**(留出段 2023-01–2026-04,零成本口径)
   **通过 ⟺ 年化超额 ≥ +3.00pp(对照 500 组中位数)且单尾 p < 0.05。**
   **与第一五二/一五三/一五四/一五五节同一道门槛,一个字不放宽。**

C3 **与第一五五节的并排对比(必须报,不参与判定)**
   第一五五节 RPS60 实测:留出段年化 +15.83%、最大回撤 16.7%、
   对照中位 +18.29%、**超额 −2.46pp、p 0.656**。
   本节必须把 RPS50 下的同四个数并排列出。**变好变坏都照报。**

C4 描述(不参与判定):逐年超额、平均持仓、止损触发次数、平均持有天数、
   双边 0.2% 成本口径、买入日 20 日波动率中位与止损标准差倍数(第一五五节 B3 的两项)。

**判据写法自律**:一律绝对阈值,不写比值判据(第一五四节 A3 的教训)。

事前预测
--------
**本节不下预测**(第一一九节起的约定)。**只登记判据。**

不做的
------
不改 `consolidation_screener.py`;不调三阈值 / MAXPOS / HOLD_MAX / 止损口径 / 排序键;
**跑完不许回头改参数再跑**;不重判第六十一节;不加第二套规则(避免 best-of-N);
不新增顶层目录;不 force push;**不往 quant-research-dev / etf-netflow-dev 推**;
不作任何可交易性声明。**若 C2 不过,如实写「统一到 RPS50 之后依然没做到」。**
"""

from __future__ import annotations

import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)
os.environ.setdefault("OXQ_STRONG_N", "50")

import platform_pivot  # noqa: E402

if __name__ == "__main__":
    assert platform_pivot.STRONG_N == 50, (
        f"本节必须在 RPS50 下跑,当前 STRONG_N={platform_pivot.STRONG_N}")
    platform_pivot.main()
